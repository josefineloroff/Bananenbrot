import { createAction } from 'redux-actions'

const Actions = {
  XXX: Symbol(),
  XXX: Symbol(),
}

export default Actions

export const handler = createAction(Actions.XXX)
export const handler = createAction(Actions.XXX)
